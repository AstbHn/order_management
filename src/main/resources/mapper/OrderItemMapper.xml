<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.astb.order.domain.orderitem.OrderItemMapper">

    <insert id="insertOrderItem">
        INSERT INTO order_item_t
        (o_id, m_id, m_name, m_price, oi_cnt, oi_total)
        VALUES
        (#{orderId}, #{menuId}, #{menuName}, #{menuPrice}, #{quantity}, #{totalPrice})
    </insert>
</mapper>