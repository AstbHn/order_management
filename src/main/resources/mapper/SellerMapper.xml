<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.astb.order.dto.SellerMapper">
    <!-- 가게별 총 판매금액 -->
    <select id="selectStoreTotalSales" resultType="long">
        SELECT
        SUM(o.o_total)
        FROM order_t o
        WHERE o.s_id = #{storeId}
    </select>

    <!-- 메뉴별 통계 -->
    <select id="selectMenuSalesByStore" resultType="com.astb.order.dto.SellerSalesDTO">
        SELECT
        oi.m_name AS menuName,
        SUM(oi.oi_cnt) AS saleCount,
        SUM(oi.oi_total) AS saleAmount
        FROM order_item_t oi
        JOIN order_t o ON oi.o_id = o.o_id
        WHERE o.s_id = #{storeId}
        GROUP BY oi.m_name
        ORDER BY saleAmount DESC
    </select>

</mapper>